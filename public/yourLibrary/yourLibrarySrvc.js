// INITILIZE SERVICE
// ============================================================
angular.module("fotify").service("yourLibrarySrvc", function($http) {

  // CRUD FUNCTIONS
  // ============================================================
  // this.getCollection = function(id) {
  //   var query = "";
  //   if (id) query = '?_id=' + id;
  //   return $http({
  //     method: 'GET',
  //     url: '/collection' + query
  //   }).then(function(response) {
  //     if (response.data.length < 2) return response.data[0];
  //     return response.data;
  //   });
  // };


  // OTHER FUNCTIONS
  // ============================================================


});
